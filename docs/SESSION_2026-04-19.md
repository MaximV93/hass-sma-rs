# Session report — 2026-04-19

Feature / stability / improvements delivered today. 5 addon releases
(0.1.41 → 0.1.45), 3 live-observed bug fixes, 1 topology discovery,
2 HA-side alerts, 1 new ADR.

## Stability — live-proven this session

| Bug | Fix release | Live evidence |
|---|---|---|
| Schema incompat: 0.1.40 rejected on existing installs because `archive:{}` was new | 0.1.41 | supervisor update accepted, sensors online |
| Yield-window left no LOGOFF → inverter held zombie session 15 min | 0.1.42 | `graceful logoff sent pkt_id=105` in logs |
| Post-yield EHOSTDOWN escalated to 10-min SLEEP_BACKOFF | 0.1.43 | 2 clean yield cycles ~2m 3s each, session_uptime 484s→2s clean transitions |
| Addon restart regenerated app_serial → inverter cached previous for 15 min → new session rejected 0x0001 | 0.1.45 | `/data/app_serial_zolder` persisted at 915024355, next restart reused cleanly |

All 4 verified against the live zolder inverter (SB 3000HF-30,
serial 2120121246) with 28 sensors active in HA.

## Features added

- **INFO log on every logon reply** (0.1.44) — groundwork for ADR 0005,
  also gives a free piconet enumeration per session start.
- **2 HA automations** (on the HA-config side):
  - `hass_sma_rs_zolder_stale_poll` — last_poll > 5 min during daylight → notify.technisch
  - `hass_sma_rs_zolder_poll_status_error` — poll_status=error for 5 min → notify.technisch
  Both live + loaded after automation reload.
- **Session Uptime sensor** (shipped 0.1.40, validated today) — essential
  for observing yield cycles cleanly. `uptime→2s` transitions show where
  reconnects happened.

## Topology discovery

Legacy haos-sbfspot logs reveal the real shape:

```
hci0 → BT Repeater @ 00:80:25:21:32:35 (serial 170004597)
         ├── SB 3000HF-30 "zolder"  serial 2120121246
         └── SB 2000HF-30 "garage"  serial 2120121383
```

Topology log from 0.1.44 shows 3 SMA-OUI MACs + host. Live probing:

- `00:80:25:21:87:CA` → reports zolder (repeater relay path A)
- `00:80:25:21:32:35` → reports zolder (configured — repeater relay path B)
- `00:80:25:1E:11:27` → EHOSTDOWN in solo probe (most likely garage's own BT, asleep-or-power-save)

**Garage is BT-reachable** — serial 2120121383 showed up in logon-broadcast
replies during the probe attempts (retcode 0x0001 "session already active"
after we left zombie sessions, but that still proves the device is
online and accepting the broadcast).

Earlier docs (including my first DISCOVER.md draft) called the second
inverter "zonneveld" — that was a misremembering. It's "garage". Fixed.

## Known limitations

- **garage not yet polled.** hass-sma-rs today is single-device per
  `inverters:` entry. ADR 0005 documents the planned multi-device
  support using per-request app_serial addressing on ONE RFCOMM
  session. Not shipped.
- **Event log query deferred** (ADR 0004). Multi-packet reply handling
  + ctrl=0xE0 + full TagList table. Not shipped, not started.
- **Archive sink not wired for this install.** TimescaleDB + CSV sinks
  exist (0.1.40) but user hasn't enabled them. MQTT + HA LTS covers
  the basics.

## Roadmap (prioritized)

1. **Multi-device polling for garage** (~2h, per ADR 0005). Gains: full
   rooftop production visibility; ~29 additional HA sensors for garage.
2. **Automated piconet probe subcommand.** Ran interactively today,
   should be a first-class CLI: `hass-sma-daemon probe-mac <addr>`.
3. **Event log** (ADR 0004). Stock-SBFspot parity. Needs multi-packet
   L2 handling — ~3h work.
4. **PVOutput upload.** Low priority; user has no current demand.
5. **Per-inverter Grafana dashboards with session-flapping alerts.**
   Foundation (15 Prometheus metrics) is in place.

## Tests + tooling

- 64 workspace tests green (was 54 yesterday)
- 2 new regression tests: `graceful_close_emits_logoff`,
  `graceful_close_safe_when_not_logged_in`
- MockTransport now Clone → handles can survive move into `Session::new`
- Clippy `-D warnings` clean on Rust 1.95
- 5 CI builds succeeded today (Builder workflow in hassio-addons repo)

## Production

zolder live throughout (aside from ~10 min collateral damage during the
MIS probe experiment). AC peaks so far today 104W @ 09:35 local.
Expected peak ~2kW+ around 14:00 local if clear weather holds.
